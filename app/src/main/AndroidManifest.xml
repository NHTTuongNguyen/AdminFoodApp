<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin_project">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FoodDetail.FoodDetail"></activity>
        <activity android:name=".OrderStatus.OrderDetail" />
        <activity android:name=".Test2" />
        <activity android:name=".ListCustomer.Listcustomer" />
        <activity android:name=".ShowComments.ShowComments" />
        <activity android:name=".OrderStatus.OrderStatus" />
        <activity android:name=".Test" />
        <activity android:name=".FoodList.FoodList" />
        <activity
            android:name=".Home.Home"
            android:label="@string/title_activity_home" />
        <activity android:name=".Screen.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>